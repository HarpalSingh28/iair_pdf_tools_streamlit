# Copy this into Streamlit Cloud → App → Settings → Secrets.
# Do not commit real passwords to GitHub.
APP_PASSWORD = "change-this-password"

# Alternative format supported by app.py:
# [auth]
# password = "change-this-password"
